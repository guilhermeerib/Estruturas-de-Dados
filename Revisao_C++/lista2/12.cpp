#include <iostream>
using namespace std;

int main() {
    int arr[10];
    cout << "Tamanho total do array: " << sizeof(arr) << " bytes" << endl;
    cout << "Tamanho de cada elemento: " << sizeof(arr[0]) << " bytes" << endl;
    return 0;
}
